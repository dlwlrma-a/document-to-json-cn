interface:
  display_name: "文档结构化提取器"
  short_description: "按 JSON Schema 提取文档字段，支持分块预检和校验"
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  brand_color: "#B45309"
  default_prompt: "Use $document-to-json-cn to extract this document into validated JSON without inventing missing facts."
policy:
  allow_implicit_invocation: true
